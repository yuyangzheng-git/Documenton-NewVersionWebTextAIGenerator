# Dify 工作流配置说明

## 1. 大纲生成工作流（Planner）

### 输入变量配置

| 变量名 | 类型 | 必填 | 说明 | 示例值 |
|-------|------|------|------|--------|
| `topic` | Text | 是 | 文档主题 | "AI XDR 主动防御体系建设方案" |
| `style` | Text | 否 | 文档风格 | "专业严肃" |
| `files` | Array | 否 | 附件列表 | [] |

### 输出变量配置

| 变量名 | 类型 | 说明 |
|-------|------|------|
| `text` / `output` / `result` | Text | JSON 格式的大纲数组 |

**输出格式要求：**

```json
[
  {
    "id": "1",
    "title": "第一章：项目背景与目标",
    "level": 1,
    "content": "",
    "requirements": "本章首先要分析行业网络安全发展趋势..."
  },
  {
    "id": "1-1",
    "title": "1.1 建设背景",
    "level": 2,
    "content": "",
    "requirements": "本小节主要描述行业网络安全威胁的发展趋势..."
  }
]
```

**注意事项：**
- 必须输出纯 JSON 字符串，不要包裹 Markdown 代码块
- `requirements` 字段必须包含章节内容规划
- 使用扁平数组结构，不是嵌套对象

---

## 2. 章节写作工作流（Chapter Writer）

### 输入变量配置

| 变量名 | 类型 | 必填 | 说明 | 示例值 |
|-------|------|------|------|--------|
| `context_summary` | Text | 是 | 文档摘要（用于上下文） | "第一章：项目背景与目标" |
| `document_topic` | Text | 是 | 文档主题 | "AI XDR 主动防御体系建设方案" |
| `section_title` | Text | 是 | 当前章节标题 | "1.1 行业与安全形势分析" |
| `full_outline` | Text | 是 | 完整大纲文本 | "1 第一章：项目背景与目标\n1.1 行业与安全形势分析..." |
| **`requirements`** | **Text** | **否** | **章节内容规划** | "本小节主要描述行业网络安全威胁的发展趋势..." |
| `files` | Array | 否 | 附件列表 | [] |

### 输出变量配置

| 变量名 | 类型 | 说明 |
|-------|------|------|
| `text` | Text | 章节内容（流式输出） |

---

## 3. 变量类型详解

### Text 类型
适用于单行或多行文本，适合所有描述性内容。

**配置方式：**
1. 在 Dify 工作流中添加"变量"节点
2. 变量名称：`requirements`
3. 变量类型：选择 `Text`
4. 必填：否（因为有些章节可能没有 requirements）
5. 默认值：可以留空

### Array 类型
适用于文件列表等复杂数据。

**配置方式：**
1. 变量类型选择 `Array`
2. 数组元素类型：根据需要选择（如 Object、Text 等）

---

## 4. requirements 变量的详细说明

### 变量类型
**Text（文本类型）**

### 为什么用 Text 类型？
- `requirements` 是章节内容规划的自然语言描述
- 是一段或多段文字说明
- 不需要结构化数据
- AI 会根据这段文字指导来生成章节内容

### requirements 内容示例

**示例 1：一级章节的 requirements**
```
本章首先要分析行业网络安全发展趋势，包括新型威胁的特点和危害。然后描述客户当前的安全现状，指出面临的主要痛点，如防护能力不足、运营效率低下等问题。最后明确项目建设的总体目标，包括构建AI XDR主动防御体系、实现威胁精准检测、快速响应等具体指标。需要注意避免在背景和目标部分过多描述具体的技术方案，将技术细节留到后续章节阐述。
```

**示例 2：二级章节的 requirements**
```
本小节主要描述行业网络安全威胁的发展趋势，包括银狐勒索病毒等新型攻击手段的特点、传播路径和危害程度。同时分析当前安全防护面临的挑战，如攻击隐蔽性强、横向移动快速、传统防护手段失效等问题。通过数据和案例说明网络安全形势的严峻性，为项目建设的必要性提供支撑。
```

### Dify 工作流中的使用方式

**在工作流的 LLM 节点中引用：**

```
# 章节写作提示词

你是专业的技术文档撰写专家。

## 文档主题
{{document_topic}}

## 当前章节
{{section_title}}

## 章节内容规划
{{requirements}}

## 完整大纲
{{full_outline}}

请根据以上信息撰写该章节内容，严格遵守章节内容规划的要求。
```

---

## 5. 验证配置是否正确

### 检查清单

- [ ] 章节写作工作流是否添加了 `requirements` 输入变量
- [ ] `requirements` 变量类型是否为 `Text`
- [ ] `requirements` 是否设为非必填（因为可能为空）
- [ ] LLM 节点是否正确引用了 `{{requirements}}` 变量
- [ ] 是否测试过生成大纲时 requirements 是否有值

### 测试方法

1. 在 Dify 控制台中打开章节写作工作流
2. 查看输入变量列表，确认包含 `requirements`
3. 运行工作流，手动输入测试数据：
   ```json
   {
     "context_summary": "第一章：项目背景与目标",
     "document_topic": "AI XDR 主动防御体系建设方案",
     "section_title": "1.1 行业与安全形势分析",
     "full_outline": "1 第一章：项目背景与目标\n1.1 行业与安全形势分析...",
     "requirements": "本小节主要描述行业网络安全威胁的发展趋势..."
   }
   ```
4. 查看生成的章节内容是否根据 requirements 编写

---

## 6. 常见问题

### Q1: requirements 为空怎么办？
**A:** 如果某个章节没有 requirements，工作流仍应正常运行。可以在提示词中添加默认指令：
```
{% if requirements %}
章节内容规划：
{{requirements}}

请根据以上规划撰写内容。
{% else %}
请根据章节标题和大纲结构撰写内容。
{% endif %}
```

### Q2: requirements 太长怎么办？
**A:** Text 类型可以支持长文本（通常数千字），如果超出限制，可以：
- 在前端截断部分内容
- 在 Dify 中使用分段生成
- 或者使用分段提示词

### Q3: 可以用其他类型吗？
**A:** 不建议。requirements 是自然语言描述，Text 类型最合适。如果需要结构化数据（如 JSON），可以考虑 JSON 类型，但会增加复杂度。

---

## 7. 前端代码调用示例

```typescript
// 调用章节写作 API
await generateSectionWithWorker(
  apiKey,
  sectionTitle,
  documentTopic,
  fullOutline,
  onChunk,
  onComplete,
  requirements,  // Text 类型，可能为空
  onError
);
```

API 会将 requirements 作为字符串传递给 Dify 工作流。
