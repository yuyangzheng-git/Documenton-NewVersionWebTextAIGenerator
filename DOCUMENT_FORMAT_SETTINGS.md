# 文档格式设置说明

## 默认格式配置

本文档说明编辑器中各种块类型的默认格式样式，符合中文文档规范。

## 段落格式

### 普通段落 (paragraph)

| 属性 | 值 | 说明 |
|-----|-----|------|
| 字号 | 15px | 标准正文字号 |
| 行高 | 1.8 | 中文标准行距 |
| 段后距 | 8px | 段落间距 |
| **首行缩进** | **2em** | **每段首行缩进2字符** |

**说明：**
- 首行缩进 2em 是中文文档的标准格式
- 相当于缩进两个中文字符的宽度
- 自动应用于所有普通段落

### 引用块 (quote)

| 属性 | 值 | 说明 |
|-----|-----|------|
| 字号 | 15px | 与正文一致 |
| 行高 | 1.8 | 中文标准行距 |
| 段后距 | 12px | 引用块间距 |
| 字体样式 | italic | 斜体 |
| 文字颜色 | rgba(55, 53, 47, 0.8) | 稍浅的颜色 |
| 左边距 | 16px | 引用块左边距 |
| 左边框 | 3px solid rgba(55, 53, 47, 0.2) | 引用线 |
| **首行缩进** | **2em** | **首行缩进2字符** |

## 列表格式

### 无序列表 (bullet)

| 属性 | 值 | 说明 |
|-----|-----|------|
| 字号 | 15px | 与正文一致 |
| 行高 | 1.8 | 列表行距 |
| 段后距 | 8px | 列表项间距 |
| 左边距 | 24px | 列表标记后距 |
| 首行缩进 | 无 | 列表项不需要首行缩进 |

### 有序列表 (numbered)

| 属性 | 值 | 说明 |
|-----|-----|------|
| 字号 | 15px | 与正文一致 |
| 行高 | 1.8 | 列表行距 |
| 段后距 | 8px | 列表项间距 |
| 左边距 | 24px | 列表标记后距 |
| 首行缩进 | 无 | 列表项不需要首行缩进 |

**说明：**
- 列表项已有标记（• 或数字），不需要首行缩进
- 左边距 24px 确保内容与标记对齐

## 标题格式

### 一级标题 (h1)

| 属性 | 值 | 说明 |
|-----|-----|------|
| 字号 | 22px | 大标题 |
| 字重 | 600 | 半粗体 |
| 行高 | 1.6 | 标题行距 |
| 段后距 | 16px | 标题下间距 |
| 段前距 | 24px | 标题上间距 |
| 首行缩进 | 无 | 标题不需要缩进 |

### 二级标题 (h2)

| 属性 | 值 | 说明 |
|-----|-----|------|
| 字号 | 18px | 中标题 |
| 字重 | 600 | 半粗体 |
| 行高 | 1.6 | 标题行距 |
| 段后距 | 14px | 标题下间距 |
| 段前距 | 20px | 标题上间距 |
| 首行缩进 | 无 | 标题不需要缩进 |

### 三级标题 (h3)

| 属性 | 值 | 说明 |
|-----|-----|------|
| 字号 | 16px | 小标题 |
| 字重 | 600 | 半粗体 |
| 行高 | 1.6 | 标题行距 |
| 段后距 | 12px | 标题下间距 |
| 段前距 | 16px | 标题上间距 |
| 首行缩进 | 无 | 标题不需要缩进 |

## 中文文档格式规范

### 首行缩进

**标准：** 每段首行缩进 2 个中文字符

**实现方式：**
```css
text-indent: 2em;
```

**注意事项：**
- 仅应用于正文段落和引用块
- 标题不缩进
- 列表项不缩进（已有列表标记）

### 行高

**标准：** 正文行高 1.8，标题行高 1.6

**实现方式：**
```css
line-height: 1.8; /* 正文 */
line-height: 1.6; /* 标题 */
```

### 段间距

**标准：**
- 正文段落：8px
- 引用块：12px
- 标题：根据层级递减（16px → 14px → 12px）

## 修改格式的方法

### 方法 1：修改组件样式

编辑 `components/NotionBlock.tsx` 中的 `getBlockStyles()` 函数：

```typescript
default: {
  fontSize: '15px',
  lineHeight: 1.8,
  marginBottom: '8px',
  textIndent: '2em', // 修改这里的值
}
```

### 方法 2：全局样式

可以在全局 CSS 中覆盖样式：

```css
/* 修改首行缩进 */
.text-indent-custom {
  text-indent: 2em !important;
}

/* 修改行高 */
.line-height-custom {
  line-height: 2 !important;
}
```

### 方法 3：用户自定义格式

如果需要支持用户自定义格式，可以在设置中添加格式选项：

```typescript
interface FormatSettings {
  fontSize: number;
  lineHeight: number;
  textIndent: string; // '0', '2em', '4em'
}
```

## 常见格式值

### 首行缩进选项

| 值 | 说明 | 对应字符数 |
|-----|------|-----------|
| 0 | 不缩进 | 0 字符 |
| 1em | 缩进1字符 | 1 中文字符 |
| 2em | 缩进2字符 | 2 中文字符（标准） |
| 4em | 缩进4字符 | 4 中文字符 |

### 行高选项

| 值 | 说明 |
|-----|------|
| 1.5 | 紧凑行距 |
| 1.8 | 标准行距（推荐） |
| 2.0 | 宽松行距 |

### 字号选项

| px | 对应磅值 | 适用场景 |
|----|---------|---------|
| 12px | 9pt | 小注 |
| 14px | 10.5pt | 小字 |
| 15px | 11pt | 正文（推荐） |
| 16px | 12pt | 大正文 |
| 18px | 13.5pt | 小标题 |
| 22px | 16pt | 大标题 |

## 导出格式说明

### Word 导出

导出到 Word 时，格式会自动转换为 Word 样式：

- `text-indent: 2em` → Word 首行缩进 2 字符
- `line-height: 1.8` → Word 行距 1.8 倍
- `fontSize: 15px` → Word 11pt 字号

### PDF 导出

PDF 导出会保留所有格式设置。

## 总结

当前默认格式遵循中文文档标准：

✅ 首行缩进：2em（2个中文字符）
✅ 行高：正文 1.8，标题 1.6
✅ 段间距：正文 8px，标题递减
✅ 字号：正文 15px（11pt），标题递增

这些设置确保了良好的可读性和专业的文档外观。
