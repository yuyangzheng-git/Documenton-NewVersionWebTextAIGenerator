# 富文本编辑使用指南

本指南详细说明如何在 AI 文档生成器中使用富文本格式。

## 支持的文本格式

### 1. 加粗（Bold）

**Markdown 语法**:
```
**加粗文本**
或
__加粗文本__
```

**效果**:
- 输入: `这是 **重要内容** 需要强调`
- 显示: 这是 **重要内容** 需要强调

### 2. 斜体（Italic）

**Markdown 语法**:
```
*斜体文本*
或
_斜体文本_
```

**效果**:
- 输入: `这是 *强调内容* 或 _同样效果_`
- 显示: 这是 *强调内容* 或 _同样效果_

### 3. 下划线（Underline）

**HTML 语法**:
```
<u>下划线文本</u>
```

**效果**:
- 输入: `这是 <u>下划线</u> 文本`
- 显示: 这是 <u>下划线</u> 文本

### 4. 删除线（Strikethrough）

**Markdown 语法**:
```
~~删除线文本~~
```

**效果**:
- 输入: `原文本 ~~已删除~~ 新文本`
- 显示: 原文本 ~~已删除~~ 新文本

### 5. 行内代码（Inline Code）

**Markdown 语法**:
```
`代码文本`
```

**效果**:
- 输入: `使用 `npm install` 命令`
- 显示: 使用 `npm install` 命令

## 支持的块类型

以下块类型都支持富文本格式：

- ✅ 正文段落（Paragraph）
- ✅ 标题 1（Heading 1）
- ✅ 标题 2（Heading 2）
- ✅ 标题 3（Heading 3）
- ✅ 无序列表（Bullet List）
- ✅ 有序列表（Numbered List）
- ✅ 引用块（Quote）
- ✅ 提示块（Callout）

## 使用方法

### 编辑模式

1. **点击文本块** - 进入编辑模式
2. **输入 Markdown 语法** - 直接输入格式代码
3. **点击块外部** - 退出编辑模式并显示格式化结果

### 示例：创建一个富文本段落

```
这是一段包含多种格式的文本。

这里有 **加粗的重要信息**，以及 *斜体强调*，还有 <u>下划线</u> 和 ~~删除的部分~~。

代码示例：使用 `const x = 10;` 定义变量。
```

**显示效果**:

这是一段包含多种格式的文本。

这里有 **加粗的重要信息**，以及 *斜体强调*，还有 <u>下划线</u> 和 ~~删除的部分~~。

代码示例：使用 `const x = 10;` 定义变量。

## 组合使用

可以将多种格式组合使用：

### 加粗 + 斜体

```
***既加粗又斜体***
```

### 加粗 + 下划线

```
**<u>加粗下划线</u>**
```

### 多种格式组合

```
这个 **<u>非常重要</u>** 的 *信息* 需要 `特殊处理`。
```

## 导出到 Word

所有富文本格式在导出到 Word 文档时都会完整保留：

| 格式 | Markdown | 导出效果 |
|------|----------|---------|
| 加粗 | `**text**` | **粗体** |
| 斜体 | `*text*` | *斜体* |
| 下划线 | `<u>text</u>` | <u>下划线</u> |
| 删除线 | `~~text~~` | ~~删除~~ |
| 代码 | `` `code` `` | 代码样式 |

## 常见问题

### Q: 为什么下划线要用 HTML 标签而不是 Markdown？

A: 标准 Markdown 没有定义下划线的语法，所以我们使用 HTML 标签 `<u>text</u>` 来表示下划线。这在 Markdown 中是被广泛接受的。

### Q: 我能嵌套格式吗？（比如加粗中有斜体）

A: 可以！你可以这样做：
```
**这里有 *斜体* 的加粗文本**
```

### Q: 格式没有显示是什么原因？

A: 检查以下几点：
1. 确保使用了正确的语法
2. 加粗需要用两个 `*` 或 `_`，斜体只用一个
3. 下划线必须使用 `<u>` 和 `</u>` 标签
4. 刷新页面重新加载
5. 点击块外部退出编辑模式以显示格式

### Q: 导出到 Word 时格式丢失？

A: 确保：
1. 文本在编辑器中正确显示了格式
2. 导出前点击块外部确保保存
3. 在 Word 中检查段落样式是否兼容

## 最佳实践

### 适当使用加粗

```
✅ **关键概念** - 用加粗突出重要术语
❌ **不要** **过度** **加粗** - 会降低可读性
```

### 斜体用于强调

```
✅ 这是一个 *强调* 的观点
❌ *不要* *每个* *单词* *都* *斜体*
```

### 代码要清晰

```
✅ 使用 `npm install` 命令安装依赖
❌ 使用npm install命令 - 不易识别代码
```

### 下划线用于特殊标记

```
✅ <u>重点标记</u> 的信息
❌ 频繁使用下划线 - 不够专业
```

## 技术细节

### 渲染过程

1. 用户输入 Markdown 格式的文本
2. 系统调用 `markdownToHtml()` 转换为 HTML
3. 使用 `dangerouslySetInnerHTML` 渲染到 DOM
4. 用户再次编辑时调用 `htmlToMarkdown()` 转换回 Markdown

### 安全考虑

- 所有 HTML 标签都会被转义，防止 XSS 攻击
- 只有 `<u>` 标签被明确保护用于下划线
- 其他 HTML 标签会被转义为文本显示

### 浏览器兼容性

- 加粗和斜体：所有现代浏览器
- 下划线：所有现代浏览器
- 删除线：所有现代浏览器
- 行内代码：所有现代浏览器

## 键盘快捷键

在文本框中编辑时：

- `Enter` - 新行（在某些块中会触发其他操作）
- `Tab` - 下一个块（在列表中可能有特殊行为）
- `Ctrl+B` / `Cmd+B` - 快速添加加粗（需要手动输入语法）
- `Ctrl+I` / `Cmd+I` - 快速添加斜体（需要手动输入语法）

> 注：目前仍需手动输入 Markdown 语法，快捷键支持将在未来版本中添加

---

**版本**: 1.0
**最后更新**: 2026-02-03
**相关文件**: `lib/markdown-renderer.ts`, `components/NotionBlock.tsx`
