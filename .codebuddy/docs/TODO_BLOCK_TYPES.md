# NotionBlock 类型实现 TODO 列表

## 块类型状态总览

| 类型 | 状态 | 说明 |
|------|------|------|
| `paragraph` | ✅ 已完成 | 基本文本块，支持自动调整高度 |
| `h1` | ✅ 已完成 | 一级标题，带"生成/重写"按钮 |
| `h2` | ✅ 已完成 | 二级标题，带"生成/重写"按钮 |
| `h3` | ✅ 已完成 | 三级标题 |
| `bullet` | ✅ 已完成 | 无序列表，带项目符号 |
| `numbered` | ✅ 已完成 | 有序列表（显示为圆点，需要改为数字） |
| `todo` | ✅ 已完成 | 待办事项，带复选框 |
| `code` | ✅ 已完成 | 代码块，等宽字体 |
| `quote` | ✅ 已完成 | 引用块，左边框和斜体 |
| `divider` | ✅ 已完成 | 分隔线 |
| `callout` | ✅ 已完成 | 提示块，蓝色背景 |
| `image` | ✅ 已完成 | 图片块，支持拖拽和按钮上传 |
| `table` | ✅ 已完成 | 表格块，3x3 默认，支持行列增删 |
| `guide` | ✅ 已完成 | 写作指导块，可折叠 |

---

## 待改进项

### 1. `numbered` 类型（有序列表）

**当前问题：** 显示为圆点（•），应该显示为数字

**位置：** `components/NotionBlock.tsx` 行 779

**修复方案：**
```typescript
case 'numbered':
  // 需要根据当前块的索引计算序号
  // 可以通过 props 传递 index 或者在父组件中计算
```

### 2. `paragraph` 类型增强

**当前状态：** 基本功能完成

**可改进项：**
- [ ] 支持富文本格式（粗体、斜体、链接等）
- [ ] 支持行内代码
- [ ] 支持内联图片

### 3. `table` 类型问题

**当前问题：** 表格可能不显示

**需要验证：**
- [ ] 测试 `/` 命令选择表格功能
- [ ] 测试表格控制按钮功能
- [ ] 测试表格 HTML 编辑功能
- [ ] 测试从 Markdown 生成的表格显示

### 4. `code` 类型增强

**当前状态：** 基本代码块

**可改进项：**
- [ ] 支持语法高亮
- [ ] 显示语言标签
- [ ] 支持代码复制按钮

### 5. `image` 类型增强

**当前状态：** 支持 URL 和拖拽上传

**可改进项：**
- [ ] 支持图片编辑（裁剪、旋转等）
- [ ] 支持图片描述编辑
- [ ] 支持图片大小调整
- [ ] 添加图片删除按钮

### 6. `guide` 类型优化

**当前状态：** 基本功能完成

**可改进项：**
- [ ] 添加模板选择
- [ ] 添加字数统计
- [ ] 添加提示词保存功能

---

## 新功能建议

### 高优先级

1. **键盘快捷键**
   - [ ] `Ctrl/Cmd + B` 粗体
   - [ ] `Ctrl/Cmd + I` 斜体
   - [ ] `Ctrl/Cmd + U` 下划线
   - [ ] `Ctrl/Cmd + K` 插入链接
   - [ ] `Ctrl/Cmd + Shift + C` 插入代码

2. **格式化工具栏**
   - [ ] 选中文本时显示格式化工具栏
   - [ ] 支持快捷插入块类型

3. **块操作增强**
   - [ ] 支持块复制
   - [ ] 支持块删除
   - [ ] 支持块类型快速切换
   - [ ] 支持块拖拽排序（已有基础）

### 中优先级

4. **表格功能增强**
   - [ ] 支持表格合并单元格
   - [ ] 支持表格样式调整
   - [ ] 支持表格标题行
   - [ ] 支持表格排序功能

5. **列表功能增强**
   - [ ] 支持列表嵌套
   - [ ] 支持列表折叠/展开
   - [ ] 支持列表拖拽调整顺序

6. **搜索和导航**
   - [ ] 全文搜索功能
   - [ ] 块快速导航
   - [ ] 目录生成

### 低优先级

7. **协作功能**
   - [ ] 多人协作编辑
   - [ ] 评论功能
   - [ ] 版本历史

8. **导出功能**
   - [ ] 导出为 PDF
   - [ ] 导出为 Word
   - [ ] 导出为 Markdown
   - [ ] 导出为 HTML

---

## 技术债务

1. **代码组织**
   - [ ] 将 `renderContent` 函数拆分为独立的组件
   - [ ] 提取样式常量
   - [ ] 改进类型定义

2. **性能优化**
   - [ ] 实现虚拟滚动
   - [ ] 优化大文档渲染性能
   - [ ] 减少不必要的重新渲染

3. **测试**
   - [ ] 添加单元测试
   - [ ] 添加集成测试
   - [ ] 添加 E2E 测试

4. **文档**
   - [ ] 完善组件文档
   - [ ] 添加使用示例
   - [ ] 编写 API 文档

---

## 已知问题

1. **表格不显示**
   - 状态：正在调试
   - 可能原因：条件判断问题
   - 解决方案：已修复条件判断，需要测试

2. **有序列表显示错误**
   - 状态：已识别
   - 影响：有序列表显示为圆点而非数字
   - 解决方案：需要传递索引或计算序号

3. **代码块无语法高亮**
   - 状态：已识别
   - 影响：代码可读性
   - 解决方案：集成 Prism.js 或 highlight.js

---

## 下一步行动

### 立即执行（Phase 1）

1. ✅ 修复 `numbered` 类型显示问题
2. ✅ 验证 `table` 类型功能
3. ✅ 修复表格不显示问题
4. ✅ 添加代码块语法高亮

### 短期执行（Phase 2）

5. ⬜ 实现键盘快捷键
6. ⬜ 添加格式化工具栏
7. ⬜ 优化表格功能
8. ⬜ 支持列表嵌套

### 中期执行（Phase 3）

9. ⬜ 添加搜索功能
10. ⬜ 实现导出功能
11. ⬜ 优化性能
12. ⬜ 添加测试

### 长期执行（Phase 4）

13. ⬜ 协作功能
14. ⬜ 版本历史
15. ⬜ 高级编辑功能

---

## 更新日志

### 2026-01-28
- ✅ 添加了 `table` 类型到 `BlockType`
- ✅ 修复了表格显示的条件判断
- ✅ 完成了所有 13 种基本块类型的实现
- ⬜ 创建了详细的 TODO 列表
