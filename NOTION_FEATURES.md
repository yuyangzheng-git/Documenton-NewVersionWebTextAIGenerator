# Notion-Style Editor Features

本项目已集成 Notion 风格的编辑器功能和设计。

## 设计主题

### 颜色方案
- **主背景色**: `#ffffff` (纯白)
- **次背景色**: `#F7F6F3` (浅灰)
- **悬停背景**: `#F1F1EF` (中灰)
- **边框颜色**: `#E1E1E0` (浅灰边框)
- **主文字**: `rgba(55, 53, 47, 1)` (深色)
- **次文字**: `rgba(55, 53, 47, 0.65)` (中深色)
- **强调色**: `#2383E2` (Notion 蓝)

### 排版
- 字体: `ui-sans-serif, -apple-system, BlinkMacSystemFont, 'Segoe UI Variable Display'`
- 行高: `1.5`
- 平滑字体渲染
- **扁平化设计**: 无 transform 缩放效果

## 核心功能

### 1. 块编辑器
- 基于块的内容结构
- 每个块可独立编辑、拖拽
- 支持 H1 和 H2 两种标题级别
- 自动生成编号 (1, 1.1, 1.2, 2, 2.1...)

### 2. 拖拽排序
- 使用 DndKit 实现
- 平滑的拖拽动画
- 自动重新排序和编号更新

### 3. 文本格式化工具栏 (选中文本自动显示)
支持多种格式化选项：

#### 文本格式化
- **粗体** (⌘B)
- *斜体* (⌘I)
- <u>下划线</u> (⌘U)
- ~~删除线~~ (⌘⇧S)
- `代码` (⌘E)

#### 文本颜色
- 默认 (深灰)
- 灰色
- 棕色
- 橙色
- 黄色
- 绿色
- 蓝色
- 紫色
- 粉色
- 红色

#### 背景高亮颜色
- 无 (透明)
- 灰色高亮
- 棕色高亮
- 橙色高亮
- 黄色高亮
- 绿色高亮
- 蓝色高亮
- 紫色高亮
- 粉色高亮
- 红色高亮

#### 块类型
- 链接 (⌘K)
- 无序列表
- 有序列表
- 引用块
- 待办事项
- 分隔线

### 4. 快捷键支持
- `⌘B` - 粗体
- `⌘I` - 斜体
- `⌘U` - 下划线
- `⌘⇧S` - 删除线
- `⌘K` - 插入链接
- `⌘E` - 代码
- `/` - 打开命令菜单
- `Esc` - 关闭菜单

### 5. 斜杠命令菜单 (输入 / 触发)
支持分类命令：

#### 基础块
- `/text` - 文本块
- `/page` - 新页面
- `/bullet` - 无序列表
- `/numbered` - 有序列表
- `/todo` - 待办列表
- `/toggle` - 切换列表
- `/divider` - 分隔线

#### 转换为
- `/h1` - 大标题
- `/h2` - 中标题
- `/h3` - 小标题
- `/quote` - 引用
- `/callout` - 提示框

#### 媒体和嵌入
- `/image` - 图片
- `/video` - 视频
- `/file` - 文件
- `/embed` - 嵌入

#### 数据库
- `/table` - 表格
- `/board` - 看板
- `/calendar` - 日历

#### 操作
- `/duplicate` - 复制
- `/moveto` - 移动到
- `/archive` - 归档
- `/delete` - 删除

### 6. AI 辅助功能
- 浮动 AI 助手按钮
- 实时聊天界面
- 帮助撰写和编辑内容

### 7. 导出功能
支持多种格式导出：
- HTML (已实现)
- DOCX (计划中)
- PDF (计划中)

### 8. 响应式设计
- 适配不同屏幕尺寸
- 移动端友好
- 触摸支持

## UI 组件

### Header
- 返回按钮
- 文档标题编辑
- 快速添加 H1/H2 按钮 (扁平化)
- 更多选项菜单
- 导出菜单

### Block 卡片
- 拖拽手柄
- 状态指示器
- 自动编号
- 可编辑标题
- 可编辑内容
- 添加/删除按钮 (扁平化悬停效果)

### AI 助手面板
- 聊天界面
- 消息历史
- 输入框
- 发送按钮 (扁平化)

### 格式化工具栏
- **文本格式化** - 粗体、斜体、下划线、删除线、代码
- **文本颜色** - 10 种预设颜色
- **背景高亮** - 10 种高亮颜色
- **块类型** - 链接、列表、引用、待办、分隔线

## 状态管理

使用 Zustand 进行状态管理：
- `documentTitle`: 文档标题
- `outline`: 块数组
- `addItem`: 添加块
- `updateItem`: 更新块
- `deleteItem`: 删除块
- `reorderItems`: 重新排序

## 数据结构

```typescript
interface OutlineItem {
  id: string;
  title: string;
  level: 1 | 2;
  status: 'idle' | 'generating' | 'completed';
  content?: string;
  number?: string;
}
```

## 技术栈

- **框架**: Next.js 16.1.1 (React 19)
- **状态管理**: Zustand
- **拖拽**: DndKit
- **样式**: Tailwind CSS + CSS Variables
- **图标**: Lucide React
- **编辑器**: 自定义块编辑器

## 开发指南

### 添加新的格式化选项
在 `components/editor/FormatToolbar.tsx` 中添加新按钮，并在 `app/editor/page.tsx` 的 `handleFormat` 函数中实现逻辑。

### 添加新的快捷键
在 `lib/keyboard-shortcuts.ts` 的 `defaultShortcuts` 数组中添加新快捷键配置。

### 自定义主题
在 `app/globals.css` 的 `:root` 部分修改 CSS 变量。

## 交互说明

### 文本格式化工具栏
- **触发方式**: 选中文本后自动显示
- **位置**: 选中文本上方
- **操作**: 点击图标应用格式
- **颜色**: 点击色块改变文本颜色或背景高亮

### 斜杠命令菜单
- **触发方式**: 输入 `/` 键
- **搜索**: 支持过滤命令
- **分类**: 按功能分组显示
- **操作**: 点击命令执行对应操作

## 未来计划

- [ ] 完整的富文本编辑器
- [ ] 更多的块类型
- [ ] 实时协作
- [ ] 版本历史
- [ ] 更多导出格式
- [ ] 图片上传
- [ ] 表格编辑
- [ ] 数学公式
- [ ] 代码高亮
- [ ] Markdown 支持
