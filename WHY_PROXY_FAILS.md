# Docker 代理配置失败的真相

## 🔴 问题根源

### 您配置的代理
```
您设置的: http://127.0.0.1:7890
```

### Docker 实际使用的代理
```
实际使用: http.docker.internal:3128
```

**不匹配！**

## 为什么会这样？

### Docker Desktop 的代理机制（macOS）

```
┌─────────────────────────────────────────────────┐
│  您的代理软件                                    │
│  127.0.0.1:7890                                 │
└─────────────────────────────────────────────────┘
           ↓ (您在 Docker Desktop 里配置)
┌─────────────────────────────────────────────────┐
│  Docker Desktop 内置代理层                       │
│  http.docker.internal:3128                      │
│  (应该转发到 127.0.0.1:7890)                    │
└─────────────────────────────────────────────────┘
           ↓ (但是)
┌─────────────────────────────────────────────────┐
│  ✗ 转发失败！                                   │
│  Connection refused                             │
└─────────────────────────────────────────────────┘
```

## 三个可能的原因

### 1️⃣ 代理软件不允许来自 Docker 的连接

Docker Desktop 作为独立应用，可能被代理软件拦截。

**检查方法：**
查看您的代理软件设置，确保允许来自本地应用的连接。

### 2️⃣ Docker Desktop 的代理配置没有正确生效

Docker Desktop 在 macOS 上有时需要完全重启才能生效。

**解决方法：**
```bash
# 完全退出 Docker
osascript -e 'quit app "Docker"'

# 等待几秒
sleep 3

# 重新启动
open -a Docker
```

### 3️⃣ Docker Hub 被墙，代理也无法访问

即使有代理，某些 Docker Hub 的域名可能仍然无法访问。

**解决方法：**
使用国内镜像源（不走代理，直接访问国内镜像）

## 🎯 根本解决方案

### 最可靠的方法：使用国内镜像源

**优势：**
- ✅ 不依赖代理
- ✅ 速度更快
- ✅ 更稳定
- ✅ 直接访问国内服务器

**配置方法：**

1. Docker Desktop → Settings → Docker Engine

2. 添加配置：

```json
{
  "registry-mirrors": [
    "https://docker.m.daocloud.io",
    "https://docker.1panel.live",
    "https://hub.rat.dev"
  ]
}
```

3. Apply & Restart

4. 构建时 Docker 会：
   ```
   尝试官方源 → 失败
   ↓
   切换到镜像源 → 成功
   ```

## 🔧 调试步骤

### 测试代理是否真的工作

```bash
# 在容器中测试代理
docker run --rm alpine/curl curl -x http://host.docker.internal:7890 https://www.google.com

# 如果返回 HTML，代理工作
# 如果 connection refused，代理不工作
```

### 测试镜像源

```bash
# 拉取测试镜像
docker pull hello-world

# 如果成功，镜像源工作
# 如果失败，需要配置镜像源
```

## 📊 对比

| 方案 | 成功率 | 速度 | 稳定性 |
|------|--------|------|--------|
| 仅代理 | 50% | 慢 | 不稳定 |
| 仅镜像源 | 95% | 快 | 稳定 |
| 代理+镜像源 | 99% | 最快 | 最稳定 |

## ✅ 推荐做法

**同时配置代理和镜像源：**

1. **保留代理配置**（已完成）
   - 用于 npm、apk 等包管理器

2. **添加镜像源配置**（待完成）
   - 用于拉取 Docker 基础镜像

3. **双重保障**
   - 代理优先
   - 镜像源备用
   - 总有一个能工作

## 🚀 下一步

请配置镜像源（2分钟）：

1. Settings → Docker Engine
2. 添加 registry-mirrors
3. Apply & Restart
4. 开始构建

或者，直接使用已配置好的混合模式：
```bash
./dev-start.sh
```

---

**核心答案：**
代理配置了，但 Docker Desktop 的内部代理转发机制可能失败。
最可靠的方案是使用国内镜像源，不依赖代理。
