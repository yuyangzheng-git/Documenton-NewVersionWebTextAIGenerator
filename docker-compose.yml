version: '3.8'

services:
  ai-document-generator:
    build:
      context: .
      dockerfile: Dockerfile
    image: ai-document-generator:latest
    container_name: ai-doc-generator
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      # AI Provider Configuration (uncomment and configure as needed)
      # - OPENAI_API_KEY=${OPENAI_API_KEY}
      # - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      # - DIFY_API_KEY=${DIFY_API_KEY}
      # - DIFY_API_URL=${DIFY_API_URL}
      # - QWEN_API_KEY=${QWEN_API_KEY}
      # - ZHIPU_API_KEY=${ZHIPU_API_KEY}
      # - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
    volumes:
      # Mount custom templates (optional)
      - ./public/templates:/app/public/templates:ro
    restart: unless-stopped
    networks:
      - ai-doc-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000', (r) => process.exit(r.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  ai-doc-network:
    driver: bridge

# Optional: Add reverse proxy with Nginx
# services:
#   nginx:
#     image: nginx:alpine
#     ports:
#       - "80:80"
#       - "443:443"
#     volumes:
#       - ./nginx.conf:/etc/nginx/nginx.conf:ro
#     depends_on:
#       - ai-document-generator
#     networks:
#       - ai-doc-network
